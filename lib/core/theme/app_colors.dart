import 'package:flutter/material.dart';

class AppColors {
  // Main background color for all screens
  static const Color background = Color(0xFF121212);

  // Neon pink/purple for primary buttons, progress rings, and active accents
  static const Color primaryNeon = Color(0xFFdf00ff);

  // Background color for cards, exercise containers, and meal items
  static const Color cardBackground = Color(0xFF1c1c1c);

  // Primary text color for main headings and titles (White)
  static const Color textPrimary = Color(0xFFFFFFFF);

  // Secondary text color for subtitles, descriptions, and smaller labels
  static const Color textSecondary = Color(0xFF777777);

  // Accent color for active status, completed streaks, or success screens
  static const Color accentGreen = Color(0xFF00FF88);

  // Background color for disabled cards or locked level items
  static const Color disabledCard = Color(0xFF181818);
  static const Color timerback = Color(0xFF2F1932);
}
